name: Build Cradle
on:
workflow_dispatch:
push:
tags:
- 'v*'
jobs:
build:
strategy:
matrix:
include:
- os: ubuntu-latest
artifact_name: Cradle-Linux-x64
executable_name: Cradle.bin
- os: windows-latest
artifact_name: Cradle-Windows-x64
executable_name: Cradle.exe
- os: macos-latest
artifact_name: Cradle-macOS-arm64
executable_name: Cradle.bin
runs-on: 
matrix.ossteps:−uses:actions/checkout@v4−uses:actions/setup−python@v5with:python−version: 
′
 3.11 
′
 −name:Installsystemdependenciesif:runner.os== 
′
 Linux 
′
 run:∣sudoapt−getupdatesudoapt−getinstall−ygccpython3−dev−name:InstallPythondependenciesrun:∣pipinstallwcwidthpipinstallnuitkaordered−setzstandard−name:BuildExecutablerun:∣python−mnuitka −−standalone −−onefile −−include−module=wcwidth −−include−module=ctypes −−output−dir=dist −−remove−output Cradle.py−uses:actions/upload−artifact@v4with:name:
{{ matrix.artifact_name }}
path: dist/${{ matrix.executable_name }}
retention-days: 30
